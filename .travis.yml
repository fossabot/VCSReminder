language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install pipenv
  - pipenv sync
  - pipenv install
  - pipenv run python -m pip install --upgrade pip setuptools wheel
